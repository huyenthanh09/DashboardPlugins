// (C) 2021-2022 GoodData Corporation
import { __assign } from "tslib";
import React from "react";
import { BubbleHoverTrigger, Bubble } from "@gooddata/sdk-ui-kit";
import { useIntl } from "react-intl";
import cx from "classnames";
import { stringUtils } from "@gooddata/util";
var tooltipAlignPoints = [
    { align: "cl cr", offset: { x: 0, y: -2 } },
    { align: "cr cl", offset: { x: 0, y: 10 } },
];
export var getTooltip = function (intl, accessibilityLimitation) {
    switch (accessibilityLimitation) {
        case "forbidden":
            return intl.formatMessage({ id: "configurationPanel.drillConfig.forbiddenDashboard.tooltip" });
        case "notShared":
            return intl.formatMessage({ id: "configurationPanel.drillConfig.notSharedDashboard.tooltip" });
        default:
            return undefined;
    }
};
var DropdownItem = function (props) {
    var onClick = props.onClick, accessibilityLimitation = props.accessibilityLimitation, title = props.title, _a = props.isSelected, isSelected = _a === void 0 ? false : _a;
    var icon = accessibilityLimitation === undefined ? undefined : "gd-icon-circle-exclamation";
    var generatedTestClass = "s-dashboard-item s-" + stringUtils.simplifyText(title);
    var classNames = cx("gd-list-item gd-drill-dashboard-item", generatedTestClass, {
        "is-selected": isSelected,
    });
    return (React.createElement("div", { className: classNames, onClick: onClick, title: title },
        icon && React.createElement("span", { className: cx("gd-list-icon", icon) }),
        React.createElement("span", null, title)));
};
export var DashboardListItem = function (props) {
    var accessibilityLimitation = props.accessibilityLimitation;
    var intl = useIntl();
    var tooltip = getTooltip(intl, accessibilityLimitation);
    if (!tooltip) {
        return React.createElement(DropdownItem, __assign({}, props));
    }
    return (React.createElement(BubbleHoverTrigger, { tagName: "div", showDelay: 200, hideDelay: 0 },
        React.createElement(DropdownItem, __assign({}, props)),
        React.createElement(Bubble, { className: "bubble-primary", alignPoints: tooltipAlignPoints }, tooltip)));
};
//# sourceMappingURL=DashboardListItem.js.map