// (C) 2022 GoodData Corporation
import React from "react";
import { ParentFiltersListItem } from "./ParentFiltersListItem";
import { LoadingComponent } from "@gooddata/sdk-ui";
import { useDashboardSelector, selectSupportsElementsQueryParentFiltering, } from "../../../../../../model";
var LOADING_MASK_HEIGHT = 150;
export var ParentFiltersList = function (props) {
    var parents = props.parents, currentFilterLocalId = props.currentFilterLocalId, setParents = props.setParents, onConnectingAttributeChanged = props.onConnectingAttributeChanged, connectingAttributes = props.connectingAttributes, attributes = props.attributes;
    var isDependentFiltersEnabled = useDashboardSelector(selectSupportsElementsQueryParentFiltering);
    if (!isDependentFiltersEnabled || parents.length < 1) {
        return null;
    }
    if (!parents.length) {
        return (React.createElement(LoadingComponent, { height: LOADING_MASK_HEIGHT, className: "s-attribute-filter-dropdown-configuration-loading" }));
    }
    return (React.createElement("div", { className: "gd-infinite-list" }, parents.map(function (item, index) {
        return (React.createElement(ParentFiltersListItem, { key: item.localIdentifier, currentFilterLocalId: currentFilterLocalId, item: item, onClick: setParents, onConnectingAttributeSelect: onConnectingAttributeChanged, connectingAttributes: connectingAttributes[index], title: attributes[index].title }));
    })));
};
//# sourceMappingURL=ParentFiltersList.js.map