// (C) 2020-2022 GoodData Corporation
import React from "react";
import cx from "classnames";
import { isDrillToAttributeUrl } from "@gooddata/sdk-model";
import invariant from "ts-invariant";
import { selectAllCatalogDisplayFormsMap, useDashboardSelector } from "../../../../model";
export var AttributeUrlSectionItem = function (_a) {
    var item = _a.item, onClickHandler = _a.onClickHandler, isSelected = _a.isSelected;
    var className = cx("gd-list-item gd-menu-item gd-drill-to-attribute-url-option s-drill-to-attribute-url-option icon-hyperlink-warning", {
        "is-selected": isSelected,
    });
    invariant(isDrillToAttributeUrl(item), "must match");
    var displayForms = useDashboardSelector(selectAllCatalogDisplayFormsMap);
    var displayForm = displayForms.get(item.target.displayForm);
    var hyperlinkDisplayForm = displayForms.get(item.target.hyperlinkDisplayForm);
    return (React.createElement("div", { className: className, onClick: onClickHandler },
        React.createElement("span", { className: "gd-parameter-title" }, displayForm === null || displayForm === void 0 ? void 0 : displayForm.title),
        React.createElement("span", { className: "addon" },
            "(", hyperlinkDisplayForm === null || hyperlinkDisplayForm === void 0 ? void 0 :
            hyperlinkDisplayForm.title,
            ")")));
};
//# sourceMappingURL=AttributeUrlSectionItem.js.map