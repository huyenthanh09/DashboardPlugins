import { __read } from "tslib";
// (C) 2020-2022 GoodData Corporation
import React, { useState } from "react";
import classNames from "classnames";
import { Button, Bubble } from "@gooddata/sdk-ui-kit";
import { stringUtils } from "@gooddata/util";
import { isDarkTheme, useTheme } from "@gooddata/sdk-ui-theme-provider";
export var Parameter = function (props) {
    var name = props.name, description = props.description, detailContent = props.detailContent, iconClassName = props.iconClassName, onAdd = props.onAdd, intl = props.intl;
    var _a = __read(useState(false), 2), displayHelp = _a[0], setDisplayHelp = _a[1];
    var theme = useTheme();
    var isDark = theme && isDarkTheme(theme);
    var id = "" + stringUtils.simplifyText(name) + (description ? stringUtils.simplifyText("_" + description) : "");
    var itemClassNames = classNames("gd-list-item gd-menu-item", "s-parameter-" + id, iconClassName);
    var addButtonLabel = intl.formatMessage({
        id: "configurationPanel.drillIntoUrl.editor.addParameterButtonLabel",
    });
    return (React.createElement("div", { id: id, className: itemClassNames, onClick: function () { return onAdd(); } },
        React.createElement("span", { className: "gd-parameter-title" }, name),
        description && React.createElement("span", { className: "addon s-parameter-description" },
            "(",
            description,
            ")"),
        React.createElement(Button, { className: "gd-button gd-button-link s-parameter-add-button", value: addButtonLabel }),
        React.createElement("div", { className: "gd-list-item-tooltip" },
            React.createElement("span", { className: "gd-icon-circle-question gd-list-item-tooltip-icon s-parameter-help-icon", onMouseEnter: function () { return setDisplayHelp(true); }, onMouseLeave: function () { return setDisplayHelp(false); } }),
            displayHelp && (React.createElement(Bubble, { className: "themed-bubble " + (isDark ? "bubble-primary" : "bubble-light"), alignTo: "#" + id, alignPoints: [{ align: "cr tl" }] }, detailContent)))));
};
//# sourceMappingURL=Parameter.js.map