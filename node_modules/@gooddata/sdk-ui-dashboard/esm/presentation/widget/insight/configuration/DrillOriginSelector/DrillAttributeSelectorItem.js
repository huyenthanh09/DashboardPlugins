// (C) 2021-2022 GoodData Corporation
import React from "react";
import cx from "classnames";
import { useDashboardSelector, selectCatalogDateDatasets } from "../../../../../model";
import { areObjRefsEqual } from "@gooddata/sdk-model";
export var DrillAttributeSelectorItem = function (props) {
    var item = props.item;
    var onClick = function () {
        props.onClick(item);
        props.onCloseDropdown();
    };
    var name = item.attribute.attributeHeader.formOf.name;
    var dateAttributes = useDashboardSelector(selectCatalogDateDatasets);
    var isFromDateAttribute = dateAttributes.some(function (attribute) {
        return attribute.dateAttributes.some(function (dateAttribute) {
            return areObjRefsEqual(dateAttribute.attribute.ref, item.attribute.attributeHeader.formOf.ref);
        });
    });
    return (React.createElement("a", { onClick: onClick, className: cx("s-drill-attribute-selector-item", {
            "gd-drill-attribute-selector-list-item": !isFromDateAttribute,
            "gd-drill-attribute-date-selector-list-item": isFromDateAttribute,
        }), title: name }, name));
};
//# sourceMappingURL=DrillAttributeSelectorItem.js.map