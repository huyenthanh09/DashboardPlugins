// (C) 2022 GoodData Corporation
import React from "react";
import debounce from "lodash/debounce";
import { ScrollablePanel } from "./configuration/ScrollablePanel";
export var WidgetScrollablePanel = function (_a) {
    var className = _a.className, children = _a.children;
    var node = React.createRef();
    var isNotOnInput = function (event) {
        var target = event.target;
        return target.tagName !== "INPUT";
    };
    var onScroll = debounce(function () {
        if (node === null || node === void 0 ? void 0 : node.current) {
            // fireGoodstrapScrollEvent(node.current, window); // TODO: what should this do?
        }
    }, 20);
    var onPanelScroll = function (event) {
        if (isNotOnInput(event)) {
            onScroll();
        }
    };
    return (React.createElement(ScrollablePanel, { className: className, ref: node, onScroll: onPanelScroll }, children));
};
//# sourceMappingURL=WidgetScrollablePanel.js.map