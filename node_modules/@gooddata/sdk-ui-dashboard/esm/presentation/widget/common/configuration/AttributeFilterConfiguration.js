// (C) 2022 GoodData Corporation
import React from "react";
import { objRefToString } from "@gooddata/sdk-model";
import invariant from "ts-invariant";
import { selectAllCatalogAttributesMap, selectAttributeFilterDisplayFormsMap, selectFilterContextAttributeFilters, useDashboardSelector, } from "../../../../model";
import { AttributeFilterConfigurationItem } from "./AttributeFilterConfigurationItem";
export var AttributeFilterConfiguration = function (props) {
    var widget = props.widget;
    var attributeFilters = useDashboardSelector(selectFilterContextAttributeFilters);
    var dfMap = useDashboardSelector(selectAttributeFilterDisplayFormsMap);
    var attrMap = useDashboardSelector(selectAllCatalogAttributesMap);
    return (React.createElement("div", null, attributeFilters.map(function (filter) {
        var displayForm = dfMap.get(filter.attributeFilter.displayForm);
        invariant(displayForm, "Inconsistent state in AttributeFilterConfiguration");
        var attribute = attrMap.get(displayForm.attribute);
        invariant(attribute, "Inconsistent state in AttributeFilterConfiguration");
        return (React.createElement(AttributeFilterConfigurationItem, { key: objRefToString(displayForm.ref), displayFormRef: displayForm.ref, title: attribute.attribute.title, widget: widget }));
    })));
};
//# sourceMappingURL=AttributeFilterConfiguration.js.map