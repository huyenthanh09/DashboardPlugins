// (C) 2022 GoodData Corporation
import { widgetRef } from "@gooddata/sdk-model";
import cx from "classnames";
import React from "react";
import { DashboardItem } from "../../presentationComponents";
import { DashboardKpi } from "../kpi";
import { selectAlertByWidgetRef, useDashboardSelector, useWidgetSelection } from "../../../model";
export var DefaultDashboardKpiWidget = function (props) {
    var kpiWidget = props.kpiWidget, onError = props.onError, onFiltersChange = props.onFiltersChange, screen = props.screen, dashboardItemClasses = props.dashboardItemClasses;
    var isSelected = useWidgetSelection(widgetRef(kpiWidget)).isSelected;
    var alertSelector = selectAlertByWidgetRef(widgetRef(kpiWidget));
    var alert = useDashboardSelector(alertSelector);
    return (React.createElement(DashboardItem, { className: cx("type-kpi", dashboardItemClasses, { "is-selected": isSelected }), screen: screen },
        React.createElement(DashboardKpi, { kpiWidget: kpiWidget, alert: alert, onFiltersChange: onFiltersChange, onError: onError })));
};
//# sourceMappingURL=DefaultDashboardKpiWidget.js.map