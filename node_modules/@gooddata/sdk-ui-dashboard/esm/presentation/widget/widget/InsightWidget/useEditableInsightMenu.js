// (C) 2021-2022 GoodData Corporation
import { useMemo } from "react";
import { useIntl } from "react-intl";
import { useDashboardCustomizationsContext } from "../../../dashboardContexts";
import { getDefaultInsightEditMenuItems } from "../../insightMenu";
export var useEditableInsightMenu = function (config) {
    var insight = config.insight, widget = config.widget, closeMenu = config.closeMenu;
    var intl = useIntl();
    var insightMenuItemsProvider = useDashboardCustomizationsContext().insightMenuItemsProvider;
    var defaultMenuItems = useMemo(function () {
        return getDefaultInsightEditMenuItems(intl);
    }, [intl]);
    var menuItems = useMemo(function () {
        return insightMenuItemsProvider
            ? insightMenuItemsProvider(insight, widget, defaultMenuItems, closeMenu, "edit")
            : defaultMenuItems;
    }, [insightMenuItemsProvider, insight, widget, defaultMenuItems, closeMenu]);
    return { menuItems: menuItems };
};
//# sourceMappingURL=useEditableInsightMenu.js.map