import { __read, __spread } from "tslib";
// (C) 2019-2022 GoodData Corporation
import React from "react";
import { DASHBOARD_LAYOUT_GRID_COLUMNS_COUNT } from "../../../../_staging/dashboard/fluidLayout/config";
import { useResizeWidthStatus } from "../../LayoutResizeContext";
import { Bullet } from "./Bullet";
var BULLETS_COUNT = DASHBOARD_LAYOUT_GRID_COLUMNS_COUNT + 1;
export function BulletsBar() {
    var widthResizingStatus = useResizeWidthStatus();
    if (!widthResizingStatus.isResizingWidth) {
        return null;
    }
    return (React.createElement("div", { className: "gd-resize-bullets-bar" }, __spread(Array(BULLETS_COUNT)).map(function (_, index) { return (React.createElement(Bullet, { key: index, index: index, isInitial: widthResizingStatus.initialIndex === index, isCurrent: widthResizingStatus.currentIndex === index })); })));
}
//# sourceMappingURL=BulletsBar.js.map