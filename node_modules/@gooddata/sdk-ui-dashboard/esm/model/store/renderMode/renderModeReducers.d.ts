import { AnyAction, CaseReducer } from "@reduxjs/toolkit";
import { RenderModeState } from "./renderModeState";
import { RenderMode } from "../../../types";
export declare const renderModeReducers: {
    setRenderMode: CaseReducer<RenderModeState, {
        payload: RenderMode;
        type: string;
    }>;
    setEditRenderMode: CaseReducer<RenderModeState, AnyAction>;
    setViewRenderMode: CaseReducer<RenderModeState, AnyAction>;
};
//# sourceMappingURL=renderModeReducers.d.ts.map