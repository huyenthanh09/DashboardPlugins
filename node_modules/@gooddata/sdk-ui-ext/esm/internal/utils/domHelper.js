// (C) 2019-2022 GoodData Corporation
import { unmountComponentAtNode } from "react-dom";
export function unmountComponentsAtNodes(elementsOrSelectors, _a) {
    if (elementsOrSelectors === void 0) { elementsOrSelectors = []; }
    var _b = _a === void 0 ? {
        unmount: unmountComponentAtNode,
        documentInstance: document,
    } : _a, unmount = _b.unmount, documentInstance = _b.documentInstance;
    elementsOrSelectors.forEach(function (elementOrSelector) {
        var element = typeof elementOrSelector === "string"
            ? documentInstance.querySelector(elementOrSelector)
            : elementOrSelector;
        if (element) {
            unmount(element);
        }
    });
}
//# sourceMappingURL=domHelper.js.map